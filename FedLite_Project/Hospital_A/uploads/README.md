# Hospital A Uploads

Place local training or prediction CSV files in this folder.

The default training file expected by the config is `hospital_a_diabetes.csv`.

If you still see `diabetes_sample.csv`, treat it as a legacy sample and use `hospital_a_diabetes.csv` for the split-dataset workflow.
