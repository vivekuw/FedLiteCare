# Hospital C Uploads

Place Hospital C training or prediction CSV files in this folder.

The default training file expected by the config is `hospital_c_diabetes.csv`.

Use `hospital_c_prediction_sample.csv` to try CSV-based prediction from the GUI.

Extra demo files:
- `hospital_c_followup_screening.csv`
- `hospital_c_weekly_screening.csv`
