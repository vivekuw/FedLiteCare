# Saved Global Models

This folder stores:

- round-specific global checkpoints such as `round_001_global_model.pt`
- the latest aggregated checkpoint such as `global_model_latest.pt`
- version metadata in `version_history.json`
